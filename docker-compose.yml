services:
  mongodb:
    image: mongo:7
    container_name: prioritree-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: prioritree
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
